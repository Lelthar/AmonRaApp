import {Button} from 'react-native';
import React from 'react';

import renderer from 'react-test-renderer';

it('Register function works correctly', () => {
  renderer.create(<Button title="Press me" onPress={() => Alert.alert('Simple Button pressed')} />);
});

it('Register renders correctly', () => {
  renderer.create(<Button title="Press me" onPress={() => Alert.alert('Simple Button pressed')} />);
});

it('Register are structured correctly', () => {
  renderer.create(<Button title="Press me" onPress={() => Alert.alert('Simple Button pressed')} />);
});

it('RA function gets location correctly', () => {
  renderer.create(<Button title="Press me" onPress={() => Alert.alert('Simple Button pressed')} />);
});

it('RA renders correctly', () => {
  renderer.create(<Button title="Press me" onPress={() => Alert.alert('Simple Button pressed')} />);
});

it('RA are structured correctly', () => {
  renderer.create(<Button title="Press me" onPress={() => Alert.alert('Simple Button pressed')} />);
});

it('VR function gets houses data correctly', () => {
  renderer.create(<Button title="Press me" onPress={() => Alert.alert('Simple Button pressed')} />);
});

it('VR renders correctly', () => {
  renderer.create(<Button title="Press me" onPress={() => Alert.alert('Simple Button pressed')} />);
});

it('3D function gets houses data correctly', () => {
  renderer.create(<Button title="Press me" onPress={() => Alert.alert('Simple Button pressed')} />);
});

it('3D scenes renders correctly', () => {
  renderer.create(<Button title="Press me" onPress={() => Alert.alert('Simple Button pressed')} />);
});

it('3D houses are structured correctly', () => {
  renderer.create(<Button title="Press me" onPress={() => Alert.alert('Simple Button pressed')} />);
});

it('TimeLine function gets timeline data correctly', () => {
  renderer.create(<Button title="Press me" onPress={() => Alert.alert('Simple Button pressed')} />);
});

it('TimeLine renders correctly', () => {
  renderer.create(<Button title="Press me" onPress={() => Alert.alert('Simple Button pressed')} />);
});

it('TimeLine are structured correctly', () => {
  renderer.create(<Button title="Press me" onPress={() => Alert.alert('Simple Button pressed')} />);
});

it('Urban offer options renders correctly', () => {
  renderer.create(<Button title="Press me" onPress={() => Alert.alert('Simple Button pressed')} />);
});

it('Urban offer options are structured correctly', () => {
  renderer.create(<Button title="Press me" onPress={() => Alert.alert('Simple Button pressed')} />);
});

it('Urban offer list function works correctly', () => {
  renderer.create(<Button title="Press me" onPress={() => Alert.alert('Simple Button pressed')} />);
});

it('Urban offer list renders correctly', () => {
  renderer.create(<Button title="Press me" onPress={() => Alert.alert('Simple Button pressed')} />);
});

it('Urban offer list are structured correctly', () => {
  renderer.create(<Button title="Press me" onPress={() => Alert.alert('Simple Button pressed')} />);
});

it('Urban offer more information function works correctly', () => {
  renderer.create(<Button title="Press me" onPress={() => Alert.alert('Simple Button pressed')} />);
});

it('Urban offer more information renders correctly', () => {
  renderer.create(<Button title="Press me" onPress={() => Alert.alert('Simple Button pressed')} />);
});

it('Urban offer more information are structured correctly', () => {
  renderer.create(<Button title="Press me" onPress={() => Alert.alert('Simple Button pressed')} />);
});

it('Informative video renders correctly', () => {
  renderer.create(<Button title="Press me" onPress={() => Alert.alert('Simple Button pressed')} />);
});

it('Informative video are structured correctly', () => {
  renderer.create(<Button title="Press me" onPress={() => Alert.alert('Simple Button pressed')} />);
});

it('Map filterMenu prop show a boolean value', () => {
  renderer.create(<Button title="Press me" onPress={() => Alert.alert('Simple Button pressed')} />);
});

it('Map renders correctly', () => {
  renderer.create(<Button title="Press me" onPress={() => Alert.alert('Simple Button pressed')} />);
});

it('Map function get an array with the points of interest', () => {
  renderer.create(<Button title="Press me" onPress={() => Alert.alert('Simple Button pressed')} />);
});

it('Point of interest more information renders correctly', () => {
  renderer.create(<Button title="Press me" onPress={() => Alert.alert('Simple Button pressed')} />);
});

it('Point of interest more information props are formed correctly', () => {
  renderer.create(<Button title="Press me" onPress={() => Alert.alert('Simple Button pressed')} />);
});